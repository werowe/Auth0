// For the example, the constants were entered into the code and not
// from the backend or the configuration file

// Circle appKey
const appKey = "your_circle_app_key";

// Auth0 
const AUTH0_CLIENT_ID = 'your_auth0_cliet_id';
const AUTH0_DOMAIN = 'your_auth0_domain';
const AUTH0_REDIRECT_URI = "your_redirect_url";

//for localhost
//const AUTH0_REDIRECT_URI = window.location.href
//    .slice(0, -window.location.hash.length || window.location.href.length)
//    .slice(0, -window.location.search.length || window.location.href.length);

